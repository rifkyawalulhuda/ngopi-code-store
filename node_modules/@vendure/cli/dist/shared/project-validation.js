"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.isVendureProjectDirectory = isVendureProjectDirectory;
exports.validateVendureProjectDirectory = validateVendureProjectDirectory;
const fs = __importStar(require("fs-extra"));
const node_path_1 = __importDefault(require("node:path"));
function isVendureProjectDirectory() {
    const cwd = process.cwd();
    const hasPackageJson = fs.existsSync(node_path_1.default.join(cwd, 'package.json'));
    const hasVendureConfig = fs.existsSync(node_path_1.default.join(cwd, 'vendure-config.ts')) ||
        fs.existsSync(node_path_1.default.join(cwd, 'vendure-config.js')) ||
        fs.existsSync(node_path_1.default.join(cwd, 'src/vendure-config.ts')) ||
        fs.existsSync(node_path_1.default.join(cwd, 'src/vendure-config.js'));
    if (hasPackageJson) {
        try {
            const packageJson = JSON.parse(fs.readFileSync(node_path_1.default.join(cwd, 'package.json'), 'utf-8'));
            const dependencies = Object.assign(Object.assign({}, packageJson.dependencies), packageJson.devDependencies);
            const hasVendureDeps = Object.keys(dependencies).some(dep => dep.includes('@vendure/') || dep === 'vendure');
            return hasVendureDeps && hasVendureConfig;
        }
        catch (_a) {
            return false;
        }
    }
    return false;
}
function validateVendureProjectDirectory() {
    if (!isVendureProjectDirectory()) {
        throw new Error('Error: Not in a Vendure project directory. Please run this command from your Vendure project root.');
    }
}
//# sourceMappingURL=project-validation.js.map